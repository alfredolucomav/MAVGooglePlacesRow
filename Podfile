# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'MAVGooglePlacesRow' do
  use_frameworks!

  pod 'Eureka'
  pod 'GooglePlaces'
  target 'MAVGooglePlacesRowTests' do
    inherit! :search_paths
    # Pods for testing
  end
end
